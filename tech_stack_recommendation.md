# 《斩杀线》技术栈推荐文档

> **评估人**：GameDesigner / TechArchitect
> **评估日期**：2026-06-28
> **评估范围**：基于项目33个设计文档（叙事设计v3.2 + 玩法设计v1.1 + 关卡设计v1.1 + 美术设计v1.3）

---

## 一、项目特征分析

### 1.1 游戏核心特征

| 维度 | 分析 |
|------|------|
| **核心操作** | 拖拽滑块（6种时间块分配）+ 事件弹窗点击 + 列表浏览 |
| **交互类型** | UI密集型——滑块、弹窗、面板、列表、结算页，而非实时动作 |
| **视觉风格** | PvZ漫画厚涂——2D图片+CSS动画即可实现，无需3D渲染管线 |
| **数据结构** | JSON驱动——20角色参数 + 159事件 + 12关卡配置全部JSON/TS文件加载 |
| **计算复杂度** | 低——月度结算（加减法）+ 状态机（5级阈值检查），无物理/AI/碰撞 |
| **跨平台需求** | PC首发（Steam），未来移动端 |
| **包体预期** | 轻量级独立游戏（<50MB） |

### 1.2 关键约束

- **不需要**：3D渲染、物理引擎、AI行为树、实时多人、着色器
- **必须**：流畅拖拽、丰富的UI动画、TypeScript类型安全、Steam发布能力
- **核心挑战**：159个事件的叙事文本管理 + 20角色差异化 + 斩杀线状态机

---

## 二、推荐方案：React + TypeScript + Tauri

### 2.1 方案构成

```
┌─────────────────────────────────────────┐
│              应用层                      │
│  React 18 + TypeScript                  │
├─────────────────────────────────────────┤
│              状态层                      │
│  Zustand（轻量状态管理）                  │
├─────────────────────────────────────────┤
│              UI层                        │
│  Tailwind CSS + CSS Modules             │
│  Framer Motion（动画）                   │
│  @dnd-kit/core（拖拽）                   │
├─────────────────────────────────────────┤
│              数据层                      │
│  JSON/TS 配置文件（角色/事件/关卡）        │
├─────────────────────────────────────────┤
│              打包层                      │
│  Tauri 2.0（桌面端原生应用）              │
│  PWA（移动端未来适配）                    │
├─────────────────────────────────────────┤
│              发布平台                     │
│  Steam（PC首发）                         │
│  Web（未来）                             │
└─────────────────────────────────────────┘
```

### 2.2 技术选型明细

| 层级 | 技术 | 版本 | 用途 |
|------|------|:----:|------|
| **前端框架** | React | 18.x | UI组件化开发 |
| **类型系统** | TypeScript | 5.x | 角色/事件/关卡数据的类型安全 |
| **状态管理** | Zustand | 4.x | 游戏状态机——净资产/体力/社交/斩杀线 |
| **样式方案** | Tailwind CSS | 3.x | PvZ风格的粗黑描边/扁平色块/快速布局 |
| **动画库** | Framer Motion | 11.x | 斩杀时刻全屏效果、事件弹出、数字跳动 |
| **拖拽库** | @dnd-kit/core | 6.x | 时间块滑块拖拽交互 |
| **桌面打包** | Tauri | 2.0 | Windows/Mac/Linux原生应用 |
| **构建工具** | Vite | 5.x | 开发热重载+生产构建 |
| **数据格式** | JSON + TS文件 | - | 角色/事件/关卡配置 |

### 2.3 评估矩阵

| 维度 | 评分 | 说明 |
|------|:----:|------|
| **核心操作匹配度** | ⭐⭐⭐⭐⭐ | 拖拽滑块是React的强项——@dnd-kit成熟稳定 |
| **数据驱动适配** | ⭐⭐⭐⭐⭐ | JSON直接import，TS类型推导，开发时即发现数据错误 |
| **PvZ风格实现** | ⭐⭐⭐⭐⭐ | CSS border: 3px solid + border-radius + 扁平色块 = 完美还原 |
| **动画能力** | ⭐⭐⭐⭐⭐ | Framer Motion——斩杀时刻全屏红光/数字跳动/面板滑入 |
| **跨平台** | ⭐⭐⭐⭐ | Tauri覆盖PC三平台；移动端需PWA适配 |
| **包体大小** | ⭐⭐⭐⭐⭐ | Tauri ~3-5MB（vs Electron ~80MB） |
| **开发效率** | ⭐⭐⭐⭐⭐ | 热重载+npm生态+组件复用，迭代最快 |
| **Steam发布** | ⭐⭐⭐⭐ | Tauri原生打包+Steamworks SDK集成（社区有方案） |
| **团队门槛** | ⭐⭐⭐⭐ | 前端开发者直接上手，无需学习游戏引擎 |

---

## 三、备选方案：Godot 4

### 3.1 方案构成

```
引擎：Godot 4.x（GDScript）
UI系统：Control节点（内置UI系统）
数据：JSON文件加载
打包：Godot原生导出（PC/Mac/Linux/Mobile/Web）
```

### 3.2 评估矩阵

| 维度 | 评分 | 说明 |
|------|:----:|------|
| **核心操作匹配度** | ⭐⭐⭐ | 需要手写拖拽逻辑，没有@dnd-kit这样的成熟库 |
| **数据驱动适配** | ⭐⭐⭐⭐ | JSON解析支持，但GDScript无TS类型安全 |
| **PvZ风格实现** | ⭐⭐⭐⭐ | 2D渲染足够，但CSS的灵活性不如 |
| **动画能力** | ⭐⭐⭐⭐ | 内置AnimationPlayer，但不如Framer Motion灵活 |
| **跨平台** | ⭐⭐⭐⭐⭐ | 原生支持全平台——最强项 |
| **包体大小** | ⭐⭐⭐⭐ | ~30-50MB |
| **开发效率** | ⭐⭐⭐⭐ | GDScript易学，UI迭代不如React快 |
| **Steam发布** | ⭐⭐⭐⭐⭐ | 原生Steamworks支持，文档丰富 |
| **团队门槛** | ⭐⭐⭐ | 需要学习Godot引擎和GDScript |

### 3.3 适用场景

如果团队有Godot经验、或需要更完整的跨平台支持（尤其是移动端原生应用），Godot是合理选择。

---

## 四、不推荐的方案

| 方案 | 不推荐原因 |
|------|-----------|
| **Unity 2D** | 杀鸡用牛刀——UI密集型游戏的UI系统（UGUI/UI Toolkit）不如Web灵活，包体~100MB+，开发迭代慢 |
| **Electron** | 包体过大~80MB+，内存占用高——Tauri是更好的替代方案 |
| **纯Canvas/Phaser** | UI系统不够灵活——拖拽滑块+弹窗+列表+结算面板用Canvas手写太低效，Phaser更适合动作游戏 |
| **Cocos Creator** | 2D能力可以但社区活跃度不如Godot，UI系统不如React灵活，生态不如Web |

---

## 五、推荐方案的项目结构

```
cut-line/
├── src/
│   ├── components/          # React组件
│   │   ├── TimeBlockSlider/ # 时间块拖拽滑块
│   │   ├── EventCard/       # 事件弹窗卡片
│   │   ├── BillList/        # 账单列表
│   │   ├── CutlineStatus/   # 斩杀线状态栏
│   │   ├── Settlement/      # 月末结算
│   │   └── CharacterSelect/ # 角色选择
│   ├── stores/              # Zustand状态
│   │   ├── gameStore.ts     # 游戏主状态
│   │   ├── cutlineStore.ts  # 斩杀线状态机
│   │   └── eventStore.ts    # 事件系统
│   ├── data/                # JSON/TS数据
│   │   ├── characters.ts    # 20角色参数
│   │   ├── events.ts        # 159事件池
│   │   ├── levels.ts        # 12月关卡配置
│   │   └── assets.ts        # 资产配置
│   ├── engine/              # 游戏逻辑
│   │   ├── economy.ts       # 经济结算引擎
│   │   ├── cutline.ts       # 斩杀线状态机
│   │   ├── events.ts        # 事件触发逻辑
│   │   └── endings.ts       # 结局判定
│   ├── styles/              # 全局样式
│   │   ├── theme.ts         # 色彩体系（对齐美术设计）
│   │   └── animations.ts    # 动画定义
│   └── App.tsx
├── public/
│   ├── images/              # 美术资产
│   │   ├── characters/      # 角色半身像
│   │   ├── scenes/          # 场景背景
│   │   └── icons/           # 图标
│   └── fonts/               # Bangers/Fredoka/Nunito字体
├── src-tauri/               # Tauri原生层
│   ├── Cargo.toml
│   └── src/main.rs
├── package.json
├── vite.config.ts
└── tailwind.config.ts
```

---

## 六、开发路线图（技术视角）

| 阶段 | 技术内容 | 工期 | 产出 |
|:----:|----------|:----:|------|
| **P1** | Vite+React+TS项目搭建 → Zustand状态 → 时间块滑块组件 → 马库斯1角色 → 3月循环 → 账单结算 | 2-3周 | 可玩原型 |
| **P2** | 事件系统 → 斩杀线状态机 → 主动行动 → 资产触发 → 6角色 → 完整12月 | 3-4周 | 核心循环 |
| **P3** | 20角色 → 159事件 → 婚姻育儿 → 6结局 → LXP系统 | 4-6周 | 完整版 |
| **P4** | 美术资产接入 → Framer Motion动画 → Tauri打包 → Steam配置 → 平衡测试 | 3-4周 | 发布版 |
| **总计** | | **12-17周** | |

---

## 七、核心依赖包清单

```json
{
  "dependencies": {
    "react": "^18.3.0",
    "react-dom": "^18.3.0",
    "zustand": "^4.5.0",
    "framer-motion": "^11.0.0",
    "@dnd-kit/core": "^6.1.0",
    "@dnd-kit/sortable": "^8.0.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^5.2.0",
    "@vitejs/plugin-react": "^4.2.0",
    "tailwindcss": "^3.4.0",
    "autoprefixer": "^10.4.0",
    "postcss": "^8.4.0"
  }
}
```

---

## 八、结论

**推荐 React + TypeScript + Tauri**。

核心理由：

1. **游戏核心操作是拖拽滑块**——这是React的强项，不是游戏引擎的强项
2. **159个事件+20角色+12关卡全部JSON数据驱动**——TypeScript类型系统让开发不犯错
3. **PvZ漫画风格本质是2D图片+CSS动画**——不需要游戏引擎的渲染管线
4. **Tauri包体仅3-5MB**——适合Steam上的轻量级独立游戏
5. **开发迭代速度最快**——热重载+npm生态+前端工具链

> 如果团队已有Godot经验，Godot 4是可接受的备选方案。但针对本项目的UI密集型特征，React方案的开发效率和灵活性显著优于游戏引擎。
